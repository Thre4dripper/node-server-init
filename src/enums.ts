export enum InstallationType {
    All = "all",
    Custom = "custom"
}

export enum ProjectType {
    Typescript = 'ts',
    Javascript = 'js'
}

export enum ApiType {
    GET = "get",
    POST = "post",
    PUT = "put",
    DELETE = "delete",
    PATCH = "patch"
}

export enum Database {
    Mongo = "mongo",
    Mysql = "mysql",
    Postgres = "postgres",
    Sqlite = "sqlite",
    Mssql = "mssql"
}
